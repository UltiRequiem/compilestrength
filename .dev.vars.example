# Server-side variables (available at runtime)
DATABASE_URL=your-database-url-here
BETTER_AUTH_SECRET=your-secret-here-must-be-at-least-32-characters-long
BETTER_AUTH_URL=http://localhost:3000

# Client-side variable (must be available at build time)
# This gets embedded into the client bundle during build
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000
